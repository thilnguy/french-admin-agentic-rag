services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./data/vector_db:/qdrant/storage

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
  # Optional: For future use if you want to containerize the app
  # app:
  #   build: .
  #   env_file: .env
  #   depends_on:
  #     - qdrant
  #     - redis
