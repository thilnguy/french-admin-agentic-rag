apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
  namespace: french-admin-rag
type: Opaque
stringData:
  # Replace these with real values or inject via Secret Management Tool (Vault/SealedSecrets)
  API_KEY: "change_me_in_production"
  OPENAI_API_KEY: "sk-..."
  QDRANT_API_KEY: "optional_qdrant_api_key_here"
